crashnote {

    key = ""

    # work async
    sync = false

    # no debug logging
    debug = false

    # disabled by default
    enabled = false

    about {
        name = "cn-core"
        version = "SNAPSHOT"
    }

    network {
        host = "api.crashnote.com"
        port = 80

        # work with SSL
        ssl = true
        port-ssl = 443

        timeout = 5s
    }

    filter {
        # filter common environment variables by default
        environment = [".*aws.*", ".*key.*", ".*secret.*", ".*_URL"]
    }
}